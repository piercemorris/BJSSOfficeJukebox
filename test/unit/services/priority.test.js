//const priority = require("../../../services/priorityService");

describe("increase user priority", () => {
  it("should return the same user priority if it is the max priority", () => {

  });

  it("")
  
  it("should return the correct user priority", () => {

  })
});